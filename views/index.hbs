    <div class="welcome">
        <h1>Welcome to DoggyDate!</h1>
    {{#if currentUser}}
        <div class="browseDogs">
            <a href="/dogs">Browse Dogs</a>
        </div>
        <form>
            <label for="search">Search</label>
            <input type="search" name="search" placeholder="Search" id="search">
        </form>
    {{else}}
        <h1>If you don't have an account, create one. Otherwise, log in.</h1>
    {{/if}}
    </div>